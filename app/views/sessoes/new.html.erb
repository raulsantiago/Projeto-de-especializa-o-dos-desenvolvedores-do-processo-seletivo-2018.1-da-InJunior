<h1>Login</h1>

<p><%= alert %></p>

<%= form_for :sessao, url: :login do |form| %>
<%= form.label "E-mail" %>
<%= form.email_field :email %>

<%= form.label "Senha" %>
<!-- so identifica e oculta o atributo -->
<%= form.password_field :password %>

<%= form.submit "Entrar" %>

<% end %>
